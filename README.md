# JEA's Pingpong (개인 · 휴대폰 탁구 게임)

## 무엇
- 휴대폰용 탁구(핑퐁) 게임. 단일 HTML 1개(`index.html`, 약 64KB, 제아 얼굴 PNG·소리 합성 코드 내장).
- 원본: 일반 클로드(claude.ai) 대화에서 만든 `Downloads\index.html`(2026-09-13 수신). 1차 개편본은 `_과거버전\2026-09-13_2110\index.html`.

## 파일
- `src_index.html` — 사람이 읽는 소스. 얼굴 PNG 자리는 `__FACE_LINE__` 한 줄.
- `_과거버전\face_line.js` — 얼굴 PNG base64 한 줄(`var FACE_SRC = ...`).
- `index.html` — 배포본. **직접 고치지 말고** `src_index.html`을 고친 뒤 아래 명령으로 다시 만든다.
  - `sed -e '/__FACE_LINE__/{r _과거버전/face_line.js' -e 'd}' src_index.html > index.html` (Git Bash)
- `qa_play.py <출력폴더> win|lose` — 자동으로 한 판(완벽 플레이/정지 플레이) 돌려 레벨 상승·승패·말풍선·콘솔오류를 JSON으로 출력.
- `qa_shots.py <출력폴더>` — 데스크톱·가로·작은 폰 화면 캡처.

## 2026-09-13 개편 내용(류영선 지시 7건)
1. 첫 화면에 제목 등장(글자가 차례로 떠오르는 애니메이션) · 2. 제목 "JEA's Pingpong"(Fraunces 이탤릭)
3. 살아남을수록 난이도 상승: 레벨 1~9, `압력 = 플레이시간/13 + 랠리 타수/8` 정수부+1. 레벨이 오르면 공 기본속도·가속·제아 막대 속도↑, 제아 조준오차·내 막대 폭↓(`tuning()` 함수). 레벨 칩(우측 상단)이 튀며 효과음.
4. 음악: 88bpm 로파이 재즈(Fmaj7·Em7·Dm7·Cmaj7·G7 8마디, EP 코드·베이스·킥/스네어/스윙 하이햇·멜로디·리버브·바이닐 노이즈). 레벨·랠리에 따라 템포 최대 1.45배. 효과음(타격=음계 상승, 득점, 레벨업, 승/패 팬파레).
5. 디자인: 포슬린 바탕(#F4F2EE)·잉크(#14181F)·나=파랑(#2F6BFF)·제아=코랄(#F0634F), SUIT 글꼴(별표4), 점수 42px 상단 중앙, 잔상·타격 잔광·서브 파문.
6. 제아 얼굴 cover(화면 꽉 채움)·투명도 0.22, 입이 화면 70% 높이에 오도록 배치(넓은 화면은 눈~입 구간).
7. 사람이 이기면 입 옆에 "goood" 말풍선(흰 카드·꼬리) + 얼굴 투명도 0.5로 또렷해짐.
- 부수: 빠른 공이 막대를 뚫는 문제(터널링) 방지용 경로 판정, 화면 이탈 시 음악 정지·복귀 시 재개, `window.__pp` 검증용 창.

## 배포
- GitHub 저장소 `pingpong`(공개) → GitHub Pages. 링크: https://ryuyoungsun308795.github.io/pingpong/
- 재배포: 이 폴더에서 `git add -A && git commit -m "수정" && git push` → 1~2분 뒤 반영.
- 주의: 공개 저장소이므로 주소를 아는 사람은 누구나 열 수 있음(얼굴 그림 내장).

## 검증(2026-09-13)
- qa_play.py win: 5:1 승리·레벨 9 도달·말풍선 표시·콘솔오류 0 / lose: 0:5·말풍선 없음·다시하기 후 레벨 1·점수 0:0.
- qa_shots.py: 390×844·360×740·844×390·1280×800 캡처로 겹침·잘림 확인.
- 글꼴 로드: SUIT Variable·Fraunces 모두 `document.fonts.check` True.
- 5관점 병렬 검증(사양·논리·모바일·소리·호환, 반박 검증 포함) 확정 8건 반영: 베이스 짧은 음 클릭음(감쇠를 음 길이 안으로) · 서브 방향(잃은 쪽이 받게) · 시작/결과 화면에서 ♪ 버튼 눌림(overlay pointer-events) · 가로 화면 제목–점수 겹침 · 가로 화면 얼굴 눈 잘림 · HUD 라벨 14/13/17px · 음악 스케줄러 밀림 보호 · inset 폴백. 기각 3건(설계상 동작).
- 라이브 검증: https://ryuyoungsun308795.github.io/pingpong/ 파일 md5 일치·글꼴 로드·자동 플레이 5:2 승리·말풍선·콘솔오류 0 (2026-09-13 22:30).

## 2026-09-13 2차 수정(류영선 지시 3건)
- PC 폭: 코트를 가운데 세로 기둥(최대 480px)으로 제한(`#stage max-width`), 바깥은 #E9E5DE.
- 승리 시 제아가 웃음: 입 주변 띠(얼굴 폭 60%·높이 19%)를 30조각 세로 스트립으로 나눠 양 끝을 얼굴 높이 4.5%만큼 들어 올리는 워프(`drawSmile`)·별도 층에서 합성해 겹쳐 비침 없음. 말풍선 글자 "good".
- 엄지에 막대가 가림: 내 막대를 바닥에서 15% 높이로 올리고 맨 아래 10.5%를 「미는 띠」로 표시(엄지는 띠에서 좌우로, 막대는 그 위).

## 2026-09-13 3차 수정(류영선 지시 3건) — 패치 `_과거버전\patch_2335.py`
- 내 막대: 바닥에서 20% 높이·두께 18px↑(H×2.6%). 미는 띠는 아래 13%.
- 물리: 막대 속도 `me.vx`(프레임 간 이동/dt, 평활)로 「밀며 치기」 판정 — `push=|vx|/(W×2.2)`(0~1), push>0.12이면 반발 속도 ×(1+0.35·push)(최대 1.35배)·각도가 미는 방향으로 최대 0.45rad 더 꺾임. 멈춘 채 치면 종전과 동일. 배수는 그 한 번의 반발에만 적용(제아가 받아치면 정상 속도).
- 랜덤 번쩍: 플레이 중 6~12초 간격으로 3초 동안 막대가 금빛으로 맥동(+충전음·진동·남은시간 고리). 그때 밀며 치면 ×1.5 고정 강타(「강타 ×1.5」 글자·강타음), 강타하면 번쩍 소진. 번쩍 중 멈춘 채 치면 보통 반발.
- 검증 `qa_physics.py`: 멈춤 531.7px/s · 밀며 697.2(1.311배) · 번쩍+밀며 797.6(1.500배) · 번쩍+멈춤 1.000배 / 자연 번쩍 ON 8.4s→off 11.3s, 18.7→21.7 (3초). 콘솔오류 0.
